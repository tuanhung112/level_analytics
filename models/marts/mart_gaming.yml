version: 2

models:
  - name: fct_level_design_performance
    columns:
      - name: win_rate
        tests:
          - dbt_utils.accepted_range:
              arguments:  # All test-specific logic goes here
                field: win_rate
                min_value: 0
                max_value: 100

      - name: drop_rate
        tests:
          - dbt_utils.accepted_range:
              arguments:
                field: drop_rate
                min_value: 0
                max_value: 100